# Union of every SmartLoad Python service's runtime requirements, de-duplicated
# for a single shared virtualenv in the all-in-one image. torch (CPU) is
# installed separately in the Dockerfile from the PyTorch CPU index so SB3
# resolves against it instead of pulling the CUDA wheel. locust lives in its
# own venv (see Dockerfile) to keep its Flask/Werkzeug pins off this set.
flask==3.0.3
psycopg2-binary==2.9.9
redis==5.0.4
scikit-learn==1.3.2
joblib==1.1.1
prometheus-client==0.20.0
waitress==3.0.2
docker==7.1.0
PyYAML==6.0.2
numpy==1.26.4
pandas==2.2.2
statsmodels==0.14.2
stable-baselines3>=2.3.0
sb3-contrib>=2.3.0
requests==2.32.3
flask-swagger-ui>=4.11
httpx>=0.27
gunicorn>=22.0
