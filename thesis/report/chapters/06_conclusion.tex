\chapter{Conclusion}
\label{ch:conclusion}

This report set out to design, build, and evaluate a load-management middleware that
schedules capacity ahead of demand rather than reacting after the fact, while staying
safe enough to sit in the request path of a production service. This chapter recaps the
problem and solution, states the headline results, reflects on what we learned, and lays
out a roadmap. It keeps the project's framing throughout: report each result as it is and
let the engineering reasoning behind each decision stand on its own.

\section{Summary of the Proposed Solution and Results}
\label{conc:summary}

\paragraph{The problem.} Conventional infrastructure couples three problems that
compound under load. Autoscaling is \emph{reactive}: capacity is added only after a
saturation signal has already degraded service. Routing is \emph{static}: a round-robin
or least-connections scheduler ignores which backend is healthy or fast right now, so a
single slow instance taxes a fixed fraction of every request stream. The obvious remedy,
inserting learned models to predict demand, detect anomalies, and route adaptively,
raises a third problem: an opaque model that misfires in production needs a floor
beneath it before operators will put it on the critical path.

\paragraph{The solution.} SmartLoad addresses these as a single proactive control loop
of the three planes described in \cref{ch:design_solution} and \cref{ch:impl_overview}.
An NGINX \emph{data plane} carries traffic and emits per-request telemetry. A
\emph{decision plane} of three engines (demand forecasting, anomaly detection, and
learned routing) turns that telemetry into predict-ahead decisions, which a
\emph{control plane} of an autoscaler, a policy manager, and a load-balancer sidecar
actuates by provisioning capacity and rewriting the upstream configuration. The planes
communicate over a five-channel Redis bus, so each component is decoupled and
independently testable. The defining discipline is that \emph{every learned model has a
deterministic fallback beneath it}: a moving-average forecaster, a threshold anomaly
rule, and a classical round-robin scheduler, with an operator override above the whole
loop. This is what makes a learned stack safe to ship, because the system degrades to a
well-understood baseline the moment any model is absent, slow to load, or switched off,
without losing safe behaviour.

\paragraph{The results.} The mechanism is demonstrated end to end. The loop closes
because the configuration and actuation chain is wired correctly and because the
deterministic capacity-aware heuristic and the classical scheduler do the live work, not
because any learned model drives the path. That deterministic floor is what lets each
learned engine sit in the live path safely. The detailed reading is in
\cref{ch:impl_testing} and \cref{ch:discussion}; the consolidated result follows.

\begin{itemize}
  \item \textbf{Closed-loop proactive autoscaling works.} In the committed adaptive
        benchmark run, a forecast-driven burst grew the backend pool from its idle
        baseline to the configured capacity ceiling and contracted it as demand receded,
        with time-to-react on the order of seconds. The
        forecast~$\rightarrow$~autoscaler~$\rightarrow$~sidecar loop closes against a live
        stack, the affirmative answer to the project's proactive-autoscaling question.
        % TODO(vps-rerun): verified pool range + scaling-action count from fresh VPS adaptive batch
  \item \textbf{Anomaly-driven exclusion decisively beats round-robin at equal capacity.}
        On the canonical adaptive-advantage bench, at the failure modes SmartLoad is built
        for, a hidden backend that sheds \texttt{503} but never trips NGINX's passive check
        (\texttt{B\_degrade}) and a slow-but-healthy backend (\texttt{D\_slow}), SmartLoad
        cuts errors roughly fivefold ($1.60\%$ to $1.32\%$ overall, with per-phase drops
        from $6.53\%$ to $1.21\%$ and $3.78\%$ to $0.97\%$) and the tail latency by
        $8$--$250\times$ against static round-robin, at equal capacity and robust across
        three runs. Round-robin cannot eject these backends; SmartLoad detects them
        organically and excludes them. This is the project's strongest measured result.
  \item \textbf{The deployed anomaly engine closes the gradual-degradation gap.} The
        shipped \texttt{trend\_rule} detector raises gradual-degradation $F_1$ from
        $0.000$, the score of every stateless detector on that pattern, to \BManFone{}
        (recall \BManRecall{}, precision \BManPrecision{}). Slow, sustained drift is exactly the
        failure mode a point-based rule misses. The threshold rule remains the
        deterministic fallback beneath the default, and an Isolation Forest engine
        \cite{liu2008isolationforest} is a selectable alternative. A separate
        threshold-versus-Isolation-Forest sweep, which does not involve the deployed
        detector, shows about \BManAgree{} cell agreement, with the Isolation Forest the more
        aggressive of the two.
  \item \textbf{The deterministic router matches round-robin; routing is not where the
        advantage lies.} On the canonical equal-capacity bench the deployed monotone
        heuristic \emph{ties} round-robin under uniform overload and passes every
        monotonicity probe: on a homogeneous, saturated pool an even split is provably
        optimal, so no router can beat round-robin (round-robin is in fact marginally
        ahead on the \texttt{C\_spike} phase). The trained PPO policy
        \cite{schulman2017ppo,raffin2021sb3} is an honest null result:
        round-robin-equivalent on the open-loop evaluation (mean-reward gap
        \BMrlPpoReward{}), with nothing to beat on the closed-loop bench either,
        implemented but not deployed (\texttt{RL\_POLICY=monotone}) and kept dormant in
        shadow. The routing layer's live value is parity with the classical scheduler at
        no extra risk, while the measurable gains come from exclusion and capacity holding.
  \item \textbf{The forecaster ships a reliable default with fallbacks in reserve.} The
        deployed \texttt{harmonic\_residual} forecaster records the best accuracy at
        \BMfcMape{} MAPE and lifts downstream SLA attainment by \BMfcSlaGain{}\,pp over the
        reactive baseline. The moving-average model is its deterministic fallback, and an
        ARIMA forecaster \cite{box2015timeseries} of order $(2,0,2)$ is an optional
        alternative that fits out of distribution on these synthetic series and ranks
        worst of the four.
  \item \textbf{The system is thoroughly tested.} Several hundred automated tests span the
        unit, contract, integration, and end-to-end layers, and the benchmark harness
        surfaced the very issues that had to be resolved before the loop could be observed
        closing.
\end{itemize}

Together these establish that SmartLoad's architecture is sound and its control loop
real: the deployed anomaly engine closes the gradual-degradation gap a point-based rule
misses, anomaly-driven exclusion decisively beats static round-robin at equal capacity
while fine-grained routing only ties it, the learned policy stays dormant as an honest
null result, and the deployed forecaster ships the most accurate model with fallbacks in
reserve. The closed loop is carried by the actuation chain and the deterministic
heuristics, not by any learned model running live.

\subsection{Summary of contributions}
\label{conc:contributions}

\Cref{tab:conc:contributions} names the principal contributions and the evidence for
each. Three stand out. The closed-loop autoscaling result is the demonstrated win. The
deterministic-fallback discipline is the design property that made it safe to put learned
components in the live path at all. And the honest, product-shaped evaluation of the
learned router is itself a contribution: measuring the trained policy against the
production task and reporting that it ties the strong baseline and contributes nothing
live, with the cause named, is more useful than a tuned number with the conditions
hidden.

\begin{table}[H]
  \centering
  \caption[Principal contributions and their supporting evidence]{Principal contributions and their supporting evidence, including the honest null result on the trained routing policy.}
  \label{tab:conc:contributions}
  \small
  \begin{tabularx}{\linewidth}{@{}p{4.0cm} Y@{}}
    \toprule
    \headrow
    \textbf{Contribution} & \textbf{Evidence} \\
    \midrule
    Closed-loop proactive autoscaling
      & Forecast-driven loop grows the pool from its idle baseline up to the configured
        capacity ceiling and contracts it again on a live stack (\cref{ch:impl_testing}).
        % TODO(vps-rerun): verified pool range + scaling-action count from fresh VPS adaptive batch
        \\
    Anomaly-driven exclusion beats round-robin at equal capacity
      & At its target failure modes (un-ejectable \texttt{503}-shedding and slow-but-healthy
        backends) SmartLoad cuts errors roughly fivefold and the tail by $8$--$250\times$
        against static round-robin, at equal capacity and robust across three runs
        (\cref{tab:disc:perphase}). This is the strongest measured result. \\
    Deterministic capacity-aware routing
      & The deployed monotone routing heuristic ties round-robin under uniform overload
        (where an even split is provably optimal) and passes every monotonicity probe; the
        live routing value is parity with the classical scheduler at no extra risk, not a
        routing advantage. \\
    Deterministic-fallback discipline
      & A moving-average forecaster, a threshold anomaly rule, and round-robin routing
        sit beneath each learned engine; the baseline plugin holds no artifact and
        cannot raise, so the loop degrades to a known baseline rather than failing. \\
    Honest, product-shaped evaluation of a trained router
      & The PPO policy \cite{schulman2017ppo,raffin2021sb3} is measured against the
        production routing task and reported as a null result: it is
        round-robin-equivalent on an equal-capacity pool where an even split is optimal,
        so it is implemented but not deployed (\texttt{RL\_POLICY=monotone}) and kept
        dormant in shadow mode, contributing nothing live. \\
    Gradual-degradation anomaly detection
      & The deployed \texttt{trend\_rule} detector raises gradual-degradation $F_1$ from
        $0.000$ (every stateless detector) to \BManFone{} and ships as the default; the
        threshold rule is the deterministic fallback and Isolation Forest
        \cite{liu2008isolationforest} a selectable alternative. \\
    Decoupled three-plane architecture
      & Decision and control planes sit out of the request path and communicate over a
        five-channel Redis bus, so a decision-service failure stops adaptation, not
        traffic (\cref{ch:design_solution}). \\
    Test and benchmark harness
      & Several hundred automated tests across unit, contract, integration, and
        end-to-end layers, plus reusable benchmark harnesses that surfaced the issues
        resolved before the loop was observed closing. \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{Lessons Learned}
\label{conc:lessons}

\subsection{Lessons learned}
\label{conc:lessons-learned}

Three lessons recur across the project, each shaping a decision a less careful process
would have made differently.

\paragraph{A deterministic fallback is what makes a learned component safe to deploy.}
Insisting on a classical baseline beneath every model was not a hedge but the enabling
condition. Because the threshold rule, the moving-average forecaster, and the round-robin
scheduler are always present, we could put learned routing and anomaly models into the
live request path without risking production, and measure each engine against its own
floor on a live stack rather than in an offline harness. The fallback turns ``learned
routing is risky'' from a reason not to build the system into a property the system
guarantees.

\paragraph{Building in vertical slices with tests at every layer kept the system
reliable.} We built SmartLoad as vertical slices (data, decision, and control planes),
backing each with unit, contract, integration, and end-to-end tests as it landed. Every
layer was exercised before the next depended on it, so the test suite grew alongside the
code instead of being bolted on at the end. When the benchmark harness later surfaced
real issues, the tests made them quick to pin down and fix.

\paragraph{Pairing each learned engine with a proven baseline gives both safety and a
path to improvement.} Every learned engine ships next to a classical counterpart it can
be measured against: the PPO router \cite{schulman2017ppo} beside round-robin, the ARIMA
forecaster \cite{box2015timeseries} beside the moving average, and the Isolation Forest
detector \cite{liu2008isolationforest} beside the threshold rule. This keeps the system
safe, because the baseline is always there to fall back to, and makes progress visible,
because every engine has a clear quantitative target to improve against on a live stack.

\subsection{Honest limitations}
\label{conc:limitations}

Reporting each result as it stands also means stating where the work stops.
\Cref{tab:conc:limitations} sets out the principal limitations across the model,
evaluation, deployment, and scope dimensions, each paired with the step that would close
it. None is a flaw in the architecture; each is a bounded next increment on a working
base.

\begin{table}[H]
  \centering
  \caption[Honest limitations and their closing steps]{Honest limitations across four dimensions, each with the concrete step that would close it.}
  \label{tab:conc:limitations}
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.0cm} Y Y@{}}
    \toprule
    \headrow
    \textbf{Dimension} & \textbf{Limitation} & \textbf{What would close it} \\
    \midrule
    Model
      & The trained PPO router is round-robin-equivalent because on an equal-capacity pool
        an even split is provably optimal, so its homogeneous training distribution carries
        no latency signal to learn from; it is implemented but not deployed
        (\texttt{RL\_POLICY=monotone}) and stays dormant in shadow mode, contributing
        nothing live.
      & Build a heterogeneous-capacity testbed and retrain on heterogeneous-latency traces
        in which backend identity correlates with latency, so the policy can learn to
        favour faster backends. \\
    \addlinespace
    Model
      & The optional ARIMA forecaster of order $(2,0,2)$ fits out of distribution on the
        synthetic series and ranks worst of the four engines, so it is not the deployed
        default.
      & A larger and more representative training corpus, plus order selection on
        in-distribution demand curves, before it could compete with the deployed
        \texttt{harmonic\_residual} forecaster. \\
    \addlinespace
    Control loop
      & The \texttt{harmonic\_residual} forecast follows a learned periodic shape more than
        the live offered load, predicting a roughly fixed rate across very different demand,
        so every adaptive-scaling claim rests on a controller working around a signal it
        cannot fully trust rather than on a sound forward predictor.
      & Blend the point forecast with live offered-rps and a headroom factor, re-fit it on
        load-coupled features, or replace it with a defensible EWMA-plus-trend predictor,
        validated on a diurnal scenario so the scaling decisions track demand. \\
    \addlinespace
    Control loop
      & The autoscaler ships two controllers and deploys the one without anti-flap logic;
        the structural pool flap it produced was pinned out of the benchmark
        (\texttt{MIN\_BACKENDS}) rather than removed at the design level, leaving the safe
        controller inert.
      & Consolidate to a single controller carrying the scale-in confirmation and
        hysteresis, with a regression test asserting an alternating demand signal does not
        reopen the flap. \\
    \addlinespace
    Control loop
      & Exclusion state lived across four stores (sidecar memory, detector clocks, the
        on-disk upstream, and the health table) that could desynchronise across a process
        restart and strand a recovered backend as \texttt{down}; this was closed this
        iteration but is not yet written down as a single-authority contract.
      & Formalise the health record as the sole authority for exclusion state, derive the
        rest from it and reconcile on startup, and add a compose-based coupled-loop
        integration test that asserts no healthy backend is left excluded. \\
    \addlinespace
    Routing
      & The monotone policy's hard cut near-zeroes any backend slower than three times the
        fastest, concentrating weight onto the quickest few; this is the upstream cause of
        the concentration the downstream skew clamp only bounds as a symptom.
      & Soften the cut at the source (a higher or load-aware threshold) and re-run the
        latency-monotonicity probe to confirm the policy still satisfies its monotonicity
        property. \\
    \addlinespace
    Evaluation
      & The canonical equal-capacity routing comparison (adaptive-advantage) reports three
        runs, but the closed-loop RQ4 magnitudes still come from single committed runs
        without full per-phase confidence intervals or a full-length head-to-head across
        workloads.
      & Extend the multi-run, confidence-interval reporting of the adaptive-advantage bench
        to every phase and to the closed-loop RQ4 trajectory. \\
    \addlinespace
    Deployment
      & The system runs under Docker Compose on a single host; the cloud and Kubernetes
        load-balancer adapters raise \texttt{NotImplementedError}, and there is no
        config/secret management, model-retraining pipeline, or data-plane health-check
        surface yet.
      & Build the real cloud and Kubernetes actuation paths, a configuration and secret
        store, a model-ops retraining pipeline, and data-plane health checks; Helm
        templates and raw manifests then enable a Kubernetes HPA versus SmartLoad
        autoscaling comparison \cite{k8shpa}. \\
    \addlinespace
    Scope
      & The middleware is single-tenant: no multi-tenancy, no per-tenant
        authentication or RBAC, no rate limiting or outbound event dispatch.
      & The planned Phase-2 SaaS layer described in \cref{conc:future}. \\
    \bottomrule
  \end{tabularx}
\end{table}

\paragraph{The coupling property behind these limitations.} Several of these items trace
to one structural property rather than to separate defects: SmartLoad's control loop is a
set of independent services whose decisions couple, so the system's behaviour is emergent
from that coupling rather than resident in any single component. The forecast feeds the
autoscaler, the routing policy feeds the detector that can bench the backends it
concentrates load onto, and exclusion state is shared across stores that must agree. The
benchmark made this concrete: no single-component fix moved the aggregate error rate, and
a detector-only change once regressed it because the autoscaler flapped underneath. The
honest framing is that the contribution is a \emph{coupled}
anomaly-exclusion-and-scaling loop with a characterised operating envelope, and the items
above are the path from a loop that works and is understood to one that is also proven and
guarded against regression.

\section{Future Directions}
\label{conc:future}

The delivered scope opens a roadmap whose leading items are the production-readiness gaps
the audit surfaced. The directions below are ordered by leverage on turning the
demonstrated loop into a deployable product, and each builds on what already works.

\paragraph{Build the real cloud and Kubernetes actuation paths.} The cloud and
Kubernetes load-balancer adapters are stubs that raise \texttt{NotImplementedError}
today, leaving the Docker Compose path as the only live actuation backend. The first
production step is to build these paths for real, against a managed load balancer and
Kubernetes endpoints. This is construction work, not a configuration flip.

\paragraph{Add configuration, secret, and model-ops infrastructure.} A deployable service
needs configuration and secret management in place of inline defaults, and a model-ops
retraining pipeline so the learned engines can be refreshed on real traffic rather than
shipped as static artefacts. Both are prerequisites for running the stack beyond a single
demonstration host.

\paragraph{Replace the proxy arrivals signal and add data-plane health checks.} The
forecaster currently reads an upper-band proxy for request arrivals rather than a true
arrivals signal, and the data plane has no first-class health-check surface. Wiring a
genuine arrivals feed and data-plane health checks would harden the loop against
conditions a single benchmark host does not exercise.

\paragraph{Consolidate the control loop into a single coherent path.} Three internal
clean-ups would turn the worked-around behaviours into designed ones. The autoscaler
should keep exactly one controller, carrying the scale-in confirmation and hysteresis, so
the safe path is the deployed path rather than inert code beside it. Exclusion state
should derive from a single authority, the backend-health record, with the on-disk
upstream and the in-memory sets reconciled from it on startup, closing the restart
deadlock as a contract rather than a one-off fix. And the monotone router's cut should be
softened at the source so it stops manufacturing the weight concentration the downstream
clamp now only bounds. A compose-based coupled-loop integration test that drives the
spike and asserts the invariants unit tests structurally cannot reach, namely no benching
cascade, the pool at or above quorum, and no healthy backend stuck excluded, would guard
all three against regression.

\paragraph{Extend the equal-capacity comparison, build a heterogeneous testbed, and
finish the K8s/Helm packaging.} The canonical adaptive-advantage bench already reports
three runs at equal capacity; the next step is full per-phase confidence intervals and a
heterogeneous-capacity testbed (backends with genuinely different service times), the
only setting in which a learned router could earn its keep over round-robin, where a
closed-loop, retrained PPO policy could be evaluated against the monotone heuristic.
Paired with Helm templates and raw Kubernetes manifests, it unlocks a multi-host
evaluation surface and the K8s HPA-versus-SmartLoad autoscaling comparison \cite{k8shpa} against the cloud-native
baseline. The target topology is not the deployment the system runs on today: the
evaluated stack runs under Docker Compose on a single host, as recorded in
\cref{tab:conc:limitations}. In the planned layout each service becomes
its own Deployment, stateful components move to StatefulSets or managed equivalents, and a
Kubernetes Horizontal Pod Autoscaler stands in for the SmartLoad autoscaler so the two
strategies can be measured side by side.

\paragraph{Retrain the learned engines on representative traffic.} The trained router
needs heterogeneous-latency traces in which backend identity correlates with latency
before it can improve on the deterministic heuristic, and the optional ARIMA forecaster
needs in-distribution training before it can compete with the deployed
\texttt{harmonic\_residual} model. With a model-ops pipeline in place, both can be
refreshed and re-evaluated against their deterministic counterparts.

\paragraph{Build the multi-tenant SaaS phase.} A planned Phase-2 turns the single-tenant
middleware into a managed offering: per-tenant isolation, tenant API keys and RBAC,
operator-UI authentication, rate limiting, and an outbound webhook dispatcher. These
build on the current architecture and open the path from middleware to a hosted product.
Because the shipped system is single-tenant, these designs describe target behaviour
rather than implemented features. The proposed isolation model resolves tenant identity
once at the API edge and carries it through every storage write and event channel, and a
proposed webhook delivery path serves customers who cannot subscribe to the internal
Redis bus directly.

\paragraph{Explore additional learned anomaly models.} For temporal patterns that are
only abnormal relative to their context, an LSTM-autoencoder anomaly engine
\cite{su2019omnianomaly} trades a heavier training pipeline for sequence-aware detection
that complements the point-based Isolation Forest.

\subsection{Phase-2 product roadmap}
\label{conc:roadmap}

Beyond sharpening the learned models, a separate track turns the single-tenant
middleware into a managed offering. \Cref{tab:conc:roadmap} groups the planned Phase-2
work into four themes, each extending the current architecture rather than reworking it.

\begin{table}[H]
  \centering
  \caption[Phase-2 product roadmap]{Phase-2 product roadmap, where each theme builds on the current architecture.}
  \label{tab:conc:roadmap}
  \small
  \begin{tabularx}{\linewidth}{@{}p{3.4cm} Y@{}}
    \toprule
    \headrow
    \textbf{Theme} & \textbf{Planned work} \\
    \midrule
    Multi-tenant SaaS
      & Per-tenant isolation, tenant API keys with role-based access control,
        operator-UI authentication, and per-tenant rate limiting. \\
    Load-balancer adapters
      & Build the unimplemented actuation backends, which raise
        \texttt{NotImplementedError} today, for HAProxy \cite{haproxy}, Envoy
        \cite{envoy}, a cloud application load balancer, and Kubernetes endpoints,
        alongside the shipped NGINX \cite{nginx} adapter. \\
    Event integration
      & An outbound webhook dispatcher that subscribes to the scaling and anomaly
        channels and forwards events to external systems. \\
    Packaging and deployment
      & Helm templates and raw Kubernetes manifests for a multi-host deployment and a
        Kubernetes HPA \cite{k8shpa} comparison surface. \\
    \bottomrule
  \end{tabularx}
\end{table}


\section{Closing Remarks}
\label{conc:closing}

SmartLoad set out to make adaptive load management both \emph{proactive} and
\emph{trustworthy}, and it delivers a working middleware that closes a predict-ahead
control loop end to end while keeping a deterministic floor beneath every learned
component, in the spirit of the self-managing systems the field has long argued for
\cite{kephart2003autonomic}. The autoscaling loop is demonstrated, the deployed
\texttt{trend\_rule} detector closes the gradual-degradation gap point-based rules miss
($F_1$ $0.000 \rightarrow{}$ \BManFone{}), anomaly-driven exclusion decisively beats static
round-robin at equal capacity while fine-grained routing only ties it and the trained
policy stays dormant as an honest null result, and the
deployed \texttt{harmonic\_residual} forecaster ships the most accurate model with
fallbacks in reserve. What lies ahead is a production-readiness roadmap rather than a
redesign: real cloud and Kubernetes actuation paths, a configuration and model-ops story,
a true arrivals signal, data-plane health checks, and a SaaS path that grows the
middleware into a managed offering. The architecture is sound, the results are real, and
the discipline that produced the system, namely measure against the production task, keep
a safe baseline, and build in tested slices, is what will carry it forward.
