# haproxy adapter (stub)

Placeholder for an HAProxy implementation of `LoadBalancerAdapter`. Raises `NotImplementedError` on construction in v1.

## When to implement

When a customer or integrator requests HAProxy support, open a feature issue and replace this stub with the real implementation. The adapter must pass `tests/conformance/lb_adapter/` to land.
