pytest==8.2.2
requests==2.32.3
psycopg2-binary==2.9.9
redis==5.0.4
# Required for the unit-tests job to import services/telemetry/app.py
# (parser tests live in test_telemetry_parser.py and load the module).
flask==3.0.3
# Required by the Docker-socket integration tests (test_autoscaler_dynamic_pool,
# test_t23_control_loop, test_anomaly_isolation_forest) and by the
# tests/e2e/forecast-autoscale suite, which restarts the autoscaler container
# to reset its cooldown timer before a forecast-driven scale-out.
docker==7.1.0
PyYAML==6.0.2
